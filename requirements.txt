# Core data + backend
arxiv>=2.1.0

# Agent orchestration stack (Member A)
# CrewAI is only installed on supported Python versions.
crewai>=1.0.0; python_version < "3.14"
langchain>=0.3.0

# Frontend/demo stack (Member C)
streamlit>=1.40.0
plotly>=5.24.0

# Testing
pytest>=8.0.0
